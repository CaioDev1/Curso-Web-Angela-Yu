<%- include('partials/header') %>

<h1>Home</h1>
<p> <%= homeText %> </p>
<% homePosts.forEach(function(item) { %>
    <div>
        <h1> <%= item.title %> </h1>
        <p> <%= item.text.slice(0, 100) + '...' %> <a href="/post/<%= item.title %>">Read more</a> </p>
    </div>
<% }) %>
    

<%- include('partials/footer') %>